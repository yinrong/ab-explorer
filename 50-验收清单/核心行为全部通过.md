# 验收：AB Explorer 核心行为全部通过

判据（能跑的命令）：

```bash
cd D:/dev/vscode-explorer
npm run build          # 退出码 0
node --test            # test/prefix.test.ts + test/statemachine.test.ts + test/fsops.test.ts 全绿
```

真实 UI（无法脚本化，人工/computer-use 截图确认）：

- [ ] 活动栏出现 AB Explorer 独立图标
- [ ] 打开一个含多子目录的目录，A 区显示横向标签，同前缀目录显示为 `-差异部分`
- [ ] 点击标签，B 区切换显示该目录树，默认折叠
- [ ] B 区展开的目录树足够深/多，触发滚动条 → 自动进入模式2，A 区变为多行，
      新增行选中的是刚才触发溢出的目录
- [ ] 折叠回内容变少后，A 区行数自动回退
- [ ] 右键文件/目录：新建文件、新建文件夹、重命名、删除（检查已进回收站非硬删）、
      在文件资源管理器中显示、复制路径，均生效
- [ ] 在 VS Code 外部改动文件（如用记事本新建一个文件），视图自动刷新
