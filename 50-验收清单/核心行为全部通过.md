# 验收：AB Explorer 核心行为全部通过

判据（能跑的命令，已实测全绿）：

```bash
cd ~/dev2/ab-explorer
npm run build          # 退出码 0
npm test               # 37 项：prefix / frequency / gitstatus / activity / fsops / keys / statemachine 全绿
npx tsc --noEmit        # 0 错误
npx vsce ls | grep -c 'dist/style.css'   # 1（样式表随 bundle 进包）
npx vsce ls | grep -c '测试输出'          # 0
```

已验证：扩展已 `vsce package` 打包并 `code --install-extension` 安装到本机 VS Code，
`code --list-extensions` 确认 `local.ab-explorer@0.1.0` 已启用；已安装 package.json
内容核对为正确 UTF-8（曾有一次安装出现编码损坏，重装后确认是偶发问题，非源码/打包问题）。

## 真实渲染验证（已做，非人工在 VS Code 里点，见下方"验证方法"）

请求过两次桌面自动化打开真实 VS Code 窗口都被人工拒绝了授权，所以走的是另一条能拿到
真实渲染结果的路：把 `vsce package` 出来同一份 `dist/webview.js` + `dist/style.css`
（不是另写的 mock）装进一个本地静态页面，模拟 provider.ts 那端的消息应答，喂真实的
`~/dev2` 目录列表（`readdirSync` 现读的，不是编出来的样例），用 Claude 内置浏览器
真渲染 + 真截图 + 300px 宽度（VS Code 侧边栏典型宽度）确认。截图证据：

- 前缀分组：`3d-man1`（6 项）、`ai`（7 项）、`soft_company`（29 项）、`voc-agent41`
  （23 项）各自的分组框都在 300px 宽度内正确换行，不再有内容被裁掉/看不见
- 数字尾巴压缩生效：`soft_company` 组显示为 `-agent1 -10 -11 -12...-65`，
  不再是 29 个标签每个都重复一遍 "agent"
- git 图标：换成 `codicon-source-control`（VS Code 源代码管理活动栏同款分支图标）+
  `--vscode-gitDecoration-modifiedResourceForeground` 语义色，16px 实际尺寸下
  清楚是个图标（不是圆点），颜色跟频率着色的蓝色系明显不同
- 频率着色：模拟数据里点击数+活跃度叠加后的高热度目录（如 `delegate`）确实比
  低热度的（如 `ai`）颜色更深更蓝

**未覆盖、需要人工在真实 VS Code 里点一遍的**（这条路径拿不到的部分）：

- [ ] 活动栏出现 AB Explorer 独立图标
- [ ] 右键文件/目录（含 A 区标签）：新建文件、新建文件夹、重命名、删除（检查已进回收站非硬删）、
      在文件资源管理器中显示、复制路径，均生效——这条只能在真 VS Code 里触发原生右键菜单验证
- [ ] 在 VS Code 外部改动文件（如用记事本新建一个文件），视图自动刷新
- [ ] 在 B 区连续展开目录直到出现滚动条 → 自动进入模式2，A 区变为多行——这条依赖
      真实的 DOM 布局测高（`scrollHeight`/`clientHeight`），模拟页面没跑这段交互
- [ ] 找一个有未提交改动的 git 仓库子目录，名字前出现分支图标；`git commit` 后
      （或等 3 秒缓存过期再刷新）图标消失——真实 git 状态而非模拟数据
- [ ] 顶部三个 checkbox 勾掉后对应视觉效果消失，重新打开 VS Code 后勾选状态仍保持
      上次的选择——依赖 `context.workspaceState` 跨窗口重启持久化，模拟页面不测这个
