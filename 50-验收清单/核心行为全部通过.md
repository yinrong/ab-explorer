# 验收：AB Explorer 核心行为全部通过

判据（能跑的命令，已实测全绿）：

```bash
cd ~/dev2/ab-explorer
npm run build          # 退出码 0
npm test               # 34 项：prefix / frequency / gitstatus / activity / fsops / keys / statemachine 全绿
npx tsc --noEmit        # 0 错误
npx vsce ls | grep -c 'dist/style.css'   # 1（样式表随 bundle 进包）
npx vsce ls | grep -c '测试输出'          # 0
```

已验证：扩展已 `vsce package` 打包并 `code --install-extension` 安装到本机 VS Code，
`code --list-extensions` 确认 `local.ab-explorer@0.1.0` 已启用；已安装 package.json
内容核对为正确 UTF-8（曾有一次安装出现编码损坏，重装后确认是偶发问题，非源码/打包问题）。

真实 UI（需人工在自己的 VS Code 里确认；本次已 `vsce package` + `code --install-extension`
装到本机 VS Code，但请求桌面自动化查看 VS Code 窗口时被人工拒绝了授权，未能截图闭环）：

- [ ] 活动栏出现 AB Explorer 独立图标
- [ ] 打开一个含多子目录的目录（可用 `测试输出/fixture`，含 typhur-app/typhur-web/typhur-api
      三个同前缀目录 + 深层嵌套 `a/b/c/d/e/f/g/h`），A 区显示横向标签，
      同前缀目录显示为 `-差异部分`
- [ ] 点击标签，B 区切换显示该目录树，默认折叠
- [ ] 在 B 区连续展开 `typhur-app/a/b/c/d/e/f/g` 直到出现滚动条 → 自动进入模式2，
      A 区变为多行，新增行选中的是刚才触发溢出的目录
- [ ] 折叠回内容变少后，A 区行数自动回退
- [ ] 右键文件/目录（含 A 区标签）：新建文件、新建文件夹、重命名、删除（检查已进回收站非硬删）、
      在文件资源管理器中显示、复制路径，均生效
- [ ] 在 VS Code 外部改动文件（如用记事本新建一个文件），视图自动刷新
- [ ] 用 `~/dev2` 这类目录打开：3d-man1/3d-man1-agent1.../3d-man2 不再被错误并成一组，
      同前缀目录 ≥3 个时外面有一圈细边框
- [ ] 反复点击同一个标签几次，它的背景色比其他未点过的标签更蓝更深
- [ ] 找一个最近 14 天内有提交、但从没在插件里点开过的仓库目录，它的背景色
      也比真正从没访问过的目录更蓝（验证外部 git 活跃度计入了着色）
- [ ] 找一个有未提交改动的 git 仓库子目录，名字前出现一个灰色小圆点；`git commit` 后
      （或等 3 秒缓存过期再刷新）圆点消失
- [ ] 顶部三个 checkbox（分组框/频率着色/Git 改动）勾掉后对应视觉效果消失，
      重新打开 VS Code 后勾选状态仍保持上次的选择
